# Order Validation Engine

A small Java/Maven project for modeling order validation behavior and exercising CI through Jenkins.

## Build

Run the unit tests with Maven:

```bash
mvn -B clean test
```
